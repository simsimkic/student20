<UserControl x:Class="ProjekatBolnica.View.PatientView.NotificationUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProjekatBolnica.View.PatientView"
             mc:Ignorable="d" 
             FontSize="14"
             d:DesignHeight="70" d:DesignWidth="250">
    <Border BorderThickness="1" BorderBrush="LightGray">
    <Grid Background="White" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40"></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition ></RowDefinition>
            
            </Grid.RowDefinitions>

            <Image Name="imgUC" Grid.Column="0"  Visibility="Visible"  Source="./Icons/newsIcon.png"/>
            <TextBox BorderThickness="0" Grid.Column="1" Margin="0,2" TextWrapping="Wrap" IsReadOnly="True" Text="{Binding NotificationText}"/>
        </Grid>
    </Border>
</UserControl>
